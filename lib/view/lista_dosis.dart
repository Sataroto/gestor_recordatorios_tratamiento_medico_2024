import 'package:flutter/material.dart';
import 'package:get/get.dart';

class listaDosisView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('lista dosis Page')),
      body: Container(
        child: Text('lista dosis page'),
      ),
    );
  }
}