import 'package:flutter/material.dart';
import 'package:get/get.dart';

class listaTratamientosView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('listaTratamientos Page')),
      body: Container(
        child: Text('listaTratamientos Body'),
      ),
    );
  }
}