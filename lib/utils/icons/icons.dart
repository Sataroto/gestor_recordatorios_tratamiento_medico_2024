import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

const double navIconSize = 20.0;
const EdgeInsets maginIconNavBar = EdgeInsets.only(bottom: 7);

const Icon tratamientoIconoLista= Icon(
  Icons.search,
  size: navIconSize,

);
const Icon dosisIconoLista= Icon(
  Icons.check_box,
  size: navIconSize,
);
const Icon calendarioIcono= Icon(
  Icons.calendar_month_rounded,
  size: navIconSize,
);